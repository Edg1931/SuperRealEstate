/** @type {import('next').NextConfig} */
// Intentionally minimal — Vercel auto-detects Next.js and needs no custom
// build settings here. No `output`, `basePath`, or experimental flags that
// could break the standard Vercel deploy (Root Directory = web).
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
